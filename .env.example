# Server Configuration
PORT=3000

# TTS Mode Configuration
# Options: simulation | edge | elevenlabs
# - simulation: Mock audio for testing (no service required)
# - edge: Microsoft Edge TTS (free, no API key, real audio)
# - elevenlabs: ElevenLabs (best quality, requires API key)
TTS_MODE=simulation

# ElevenLabs API Configuration (required only when TTS_MODE=elevenlabs)
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here
ELEVENLABS_API_BASE_URL=https://api.elevenlabs.io/v1

# Base URL for audio file links (update for production)
BASE_URL=http://localhost:3000

# Node Environment
NODE_ENV=development